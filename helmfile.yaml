# 루트 helmfile — 주제별 helmfile을 순서대로 실행
# 사용법:
#   helmfile diff   # 적용 전 변경사항 미리보기
#   helmfile apply  # 전체 설치/업그레이드
#   helmfile -f argocd/helmfile.yaml apply   # 특정 주제만
helmfiles:
  - path: infra/ingress-nginx/helmfile.yaml
  - path: argocd/helmfile.yaml
