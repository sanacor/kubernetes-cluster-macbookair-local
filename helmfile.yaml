# 루트 helmfile — 컴포넌트별 helmfile을 순서대로 실행
# 사용법:
#   helmfile diff   # 적용 전 변경사항 미리보기
#   helmfile apply  # 전체 설치/업그레이드
#   helmfile -f helmfiles/apps.yaml apply   # 특정 컴포넌트만
helmfiles:
  - path: helmfiles/ingress-nginx.yaml
  - path: helmfiles/argocd.yaml
  - path: helmfiles/apps.yaml
