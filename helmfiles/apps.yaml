# ArgoCD Application 등록 (GitOps 앱 목록은 values/argocd-apps.yaml에서 관리)
repositories:
  - name: argo
    url: https://argoproj.github.io/argo-helm

releases:
  - name: argocd-apps
    namespace: argocd
    chart: argo/argocd-apps
    version: 2.0.5
    values:
      - ../values/argocd-apps.yaml
